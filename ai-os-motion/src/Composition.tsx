import React from "react";
import { AbsoluteFill } from "remotion";

/** Kept for Remotion scaffold compatibility — unused. */
export const MyComposition: React.FC = () => {
  return <AbsoluteFill />;
};
